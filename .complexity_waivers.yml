# Time-boxed exceptions to the complexity budget.
#
# A waiver is the ONLY way to land a new entity that exceeds
# .rubocop_metrics.yml. It is deliberately more expensive than fixing the
# code: it needs a named owner, a reason a reviewer can argue with, and an
# expiry date. Once `expires` passes, CI fails until the entity is fixed or
# the waiver is consciously renewed — so a waiver decays into work instead
# of into permanent amnesty.
#
# Cap the expiry at 90 days out. Copy the key verbatim from the
# bin/complexity_check output.
#
# - key: "engines/collavre/app/services/collavre/example.rb | Metrics/MethodLength | Collavre::Example#call"
#   owner: "@github-handle"
#   reason: "Vendor protocol handshake is a single linear sequence; splitting it hides the order."
#   expires: "2026-11-09"

waivers:
  - key: "engines/collavre/app/services/collavre/mcp_service.rb | Metrics/AbcSize | Collavre::McpService.register_tool_from_source"
    owner: "Codex (Creative-native Ruby tools follow-up)"
    reason: "Exact revert of PR #1721 restores this pre-existing complexity; resolve in docs/plans/2026-09-23-creative-ruby-tools.md follow-up."
    expires: "2026-10-07"
  - key: "engines/collavre/app/services/collavre/mcp_service.rb | Metrics/MethodLength | Collavre::McpService.register_tool_from_source"
    owner: "Codex (Creative-native Ruby tools follow-up)"
    reason: "Exact revert of PR #1721 restores this pre-existing complexity; resolve in docs/plans/2026-09-23-creative-ruby-tools.md follow-up."
    expires: "2026-10-07"
  - key: "engines/collavre/app/services/collavre/tools/creative_retrieval_service.rb | Metrics/AbcSize | Collavre::Tools::CreativeRetrievalService#serialize_creative"
    owner: "Codex (Creative-native Ruby tools follow-up)"
    reason: "Exact revert of PR #1721 restores this pre-existing complexity; resolve in docs/plans/2026-09-23-creative-ruby-tools.md follow-up."
    expires: "2026-10-07"
  - key: "engines/collavre/app/services/collavre/tools/creative_retrieval_service.rb | Metrics/MethodLength | Collavre::Tools::CreativeRetrievalService#serialize_creative"
    owner: "Codex (Creative-native Ruby tools follow-up)"
    reason: "Exact revert of PR #1721 restores this pre-existing complexity; resolve in docs/plans/2026-09-23-creative-ruby-tools.md follow-up."
    expires: "2026-10-07"
