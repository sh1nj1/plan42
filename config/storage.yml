test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

local:
  service: Disk
  root: <%= Rails.root.join("storage") %>

<%
  # `AwsCredentials.s3` returns a source-coherent pair (both DB, both ENV, or
  # neither), so a partial admin save can't combine a DB access_key_id with an
  # ENV secret and boot Active Storage with a mismatched credential pair.
  # Defaults via `Collavre::IntegrationSettings.fetch(default: ENV[...])` keep
  # `USE_COLLAVRE_GEM=true` builds working when the gem doesn't yet register
  # these keys (fetch returns nil for unknown keys, bypassing its ENV branch).
  s3_credentials = defined?(Collavre::AwsCredentials) ? Collavre::AwsCredentials.s3 : {}
%>
amazon:
  service: S3
  access_key_id: <%= s3_credentials[:access_key_id] %>
  secret_access_key: <%= s3_credentials[:secret_access_key] %>
  region: <%= Collavre::IntegrationSettings.fetch(:aws_region, default: ENV.fetch("AWS_REGION", "ap-northeast-2")) %>
  bucket: <%= Collavre::IntegrationSettings.fetch(:aws_s3_bucket, default: ENV.fetch("AWS_S3_BUCKET", "collavre-uploads-#{Rails.env}")) %>
  upload:
    server_side_encryption: "AES256"

# Remember not to checkin your GCS keyfile to a repository
# google:
#   service: GCS
#   project: your_project
#   credentials: <%= Rails.root.join("path/to/gcs.keyfile") %>
#   bucket: your_own_bucket-<%= Rails.env %>

# Use bin/rails credentials:edit to set the Azure Storage secret (as azure_storage:storage_access_key)
# microsoft:
#   service: AzureStorage
#   storage_account_name: your_account_name
#   storage_access_key: <%= Rails.application.credentials.dig(:azure_storage, :storage_access_key) %>
#   container: your_container_name-<%= Rails.env %>

# mirror:
#   service: Mirror
#   primary: local
#   mirrors: [ amazon, google, microsoft ]
