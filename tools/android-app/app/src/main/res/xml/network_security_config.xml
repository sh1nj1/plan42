<?xml version="1.0" encoding="utf-8"?>
<!--
  Production traffic is https (collavre.com). Cleartext is allowed ONLY for the
  tailscale preview host so a debug build can talk to a local Rails preview
  server over http during E2E testing. Everything else stays https-only.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">macbook-pro.tailadceed.ts.net</domain>
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
    </domain-config>
</network-security-config>
