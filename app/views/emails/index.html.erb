<h1><%= t('emails.index_title') %></h1>
<table>
  <thead>
    <tr>
      <th><%= t('emails.list.title.to') %></th>
      <th><%= t('emails.list.title.event') %></th>
      <th><%= t('emails.list.title.sent_at') %></th>
    </tr>
  </thead>
  <tbody>
    <% @emails.each do |email| %>
      <tr>
        <td><%= link_to email.email, email_path(email) %></td>
        <td><%= t("emails.event.#{email.event}") %></td>
        <td><%= l(email.created_at, format: :short) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
